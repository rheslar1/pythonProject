name: myAction
on:
  push:
    branches: dev
  pull_request:
    branches: dev 
    
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: development

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with: 
        ref: dev
    
     
        

  
    
     
    
      
    
    
    
