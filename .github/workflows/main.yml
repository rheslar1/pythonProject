name: myAction
on:
  push:
    branches: [ $default-branch ]
  pull_request:
    
    
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: development

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with: 
        ref: dev
    
     
        

  
    
     
    
      
    
    
    
